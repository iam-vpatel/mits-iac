# ----------------------------
# Terraform-specific ignores
# ----------------------------

# Local .terraform directories
.terraform/

# Terraform state files
*.tfstate
*.tfstate.*
# Terraform state backup files
terraform.tfstate.backup
*.tfstate.backup

# Crash log (when Terraform crashes)
crash.log

# Exclude any plan output files
*.tfplan

# Variable definitions that often contain secrets
*.tfvars
*.tfvars.json

# Override files (local convenience, not for VCS)
override.tf
override.tf.json
*_override.tf

# CLI config (can contain credentials or custom settings)
.terraformrc
terraform.rc

# ----------------------------
# Common “do not commit” patterns
# ----------------------------

# IDE/editor folders and swap files
.vscode/
.idea/
*.swp
*~

# Environment-specific variables (often contain secrets)
.env
.env.*

# OS-generated files
.DS_Store
Thumbs.db

# Log files
*.log

# AWS credentials folder (if you ever store AWS creds locally)
.aws/

# ----------------------------
# Optional: other sensitive files
# ----------------------------

# If you have a backend configuration that includes secrets
# (e.g. “backend.tfvars”), you can add it here:
# backend.tfvars

# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
*.tfvars
*.tfvars.json
terraform.tfvars
override.tf
override.tf.json

# Common
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
*.swp
*~
